# Sidebar chat model. Key stays in .env, not the extension.
VITE_LLM_BASE_URL=https://openrouter.ai/api/v1
VITE_LLM_API_KEY=
VITE_LLM_MODEL=qwen/qwen3.8-max-0902

# Local server (WS /extension + HTTP /command + /v1/chat/completions)
EMBER_MCP_PORT=8765
